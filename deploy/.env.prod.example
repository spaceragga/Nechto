DOMAIN=nechto.by
POSTGRES_USER=nechto
POSTGRES_PASSWORD=replace-with-random-password
POSTGRES_DB=nechto
JWT_SECRET=replace-with-at-least-32-random-characters
JWT_EXPIRES_IN=7d
# Public URL of files served by Nest at /uploads (via Next /api rewrite).
STORAGE_PUBLIC_BASE_URL=https://nechto.by/api/uploads
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=replace
SMTP_PASSWORD=replace
SMTP_FROM=hello@nechto.by
